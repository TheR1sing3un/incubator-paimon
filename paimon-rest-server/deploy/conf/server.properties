# ============================================================
# Paimon REST Catalog Server Configuration
# ============================================================

# ---- Catalog ----
warehouse=/tmp/paimon/warehouse
metastore=filesystem

# ---- Server ----
rest-server.host=0.0.0.0
rest-server.port=26754
rest-server.prefix=paimon
rest-server.io-threads=4
rest-server.worker-threads=16
rest-server.max-content-length=10485760
rest-server.metadata.resource-id=aiDataLakeService
rest-server.metadata.jdbc-catalog=paimon_catalog

# ---- Metadata Store (MySQL) ----
# Option A: Kuaishou KsDataSource (production)
# rest-server.metadata.resource-id=paimon_catalog

# Option B: JDBC URL with HikariCP (local dev / standalone deployment)
# rest-server.metadata.jdbc-url=jdbc:mysql://localhost:3306/paimon_catalog?useSSL=false&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
# rest-server.metadata.jdbc-user=paimon
# rest-server.metadata.jdbc-password=paimon123
# rest-server.metadata.pool.max-size=10
# rest-server.metadata.pool.min-idle=2
# rest-server.metadata.pool.connection-timeout-ms=30000

# ---- Authentication (optional, not yet implemented) ----
# rest-server.auth.url=http://auth-service:8081/validate
